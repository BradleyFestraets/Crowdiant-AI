# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# ============================================================================
# AUTHENTICATION (Required)
# ============================================================================
# NextAuth secret for session encryption
# Generate with: npx auth secret OR openssl rand -base64 32
# Must be 32+ characters, keep secret!
AUTH_SECRET="generate-with-npx-auth-secret"

# Discord OAuth (optional for development, required for social login)
AUTH_DISCORD_ID="your-discord-client-id"
AUTH_DISCORD_SECRET="your-discord-client-secret"

# ============================================================================
# DATABASE (Required)
# ============================================================================
# PostgreSQL connection strings
# For local development you can use a direct URL; in production use Prisma Accelerate pooled URL.
# Direct format: postgresql://[user]:[password]@[host]:[port]/[database]
# Accelerate format example:
# prisma+postgres://accelerate-token@accelerate-host?connection_string=ENCODED_POSTGRES_URL
DATABASE_URL="postgresql://postgres:password@localhost:5432/crowdiant_dev"
# Optional: provide a DIRECT_URL when using pooled DATABASE_URL in production for migrations
DIRECT_URL="postgresql://postgres:password@localhost:5432/crowdiant_dev"

# ============================================================================
# APPLICATION (Auto-detected)
# ============================================================================
# Node environment - Next.js sets this automatically
NODE_ENV="development"

# Logging verbosity
# Options: debug, info, warn, error
LOG_LEVEL="debug"

# ============================================================================
# FUTURE INTEGRATIONS (Not needed for Epic 1)
# ============================================================================
# Stripe Connect (Epic 3: Express Checkout)
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Redis (Epic 3: Pre-auth state management)
# REDIS_URL="redis://localhost:6379"

# Sentry (Epic 1 Story 6: Observability)
# SENTRY_DSN="https://...@sentry.io/..."                # Server usage
# NEXT_PUBLIC_SENTRY_DSN="https://...@sentry.io/..."     # Client bundle
# SENTRY_AUTH_TOKEN=""                                   # CI only (source maps); NEVER expose publicly

# Twilio (Epic 9: SMS notifications)
# TWILIO_ACCOUNT_SID="..."
# TWILIO_AUTH_TOKEN="..."
# TWILIO_PHONE_NUMBER="+1..."

# Email (Epic 2 Story 2.2: Staff invitations)
RESEND_API_KEY="your-resend-api-key"

# AWS S3 (Epic 5: Menu images)
# AWS_ACCESS_KEY_ID="..."
# AWS_SECRET_ACCESS_KEY="..."
# AWS_S3_BUCKET="..."
# AWS_REGION="us-east-1"

